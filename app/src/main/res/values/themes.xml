<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CityMap" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/primary_transparent_50</item>
    </style>

    <style name="SplashTheme" parent="Base.Theme.CityMap">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="Theme.CityMap" parent="Base.Theme.CityMap" />
</resources>